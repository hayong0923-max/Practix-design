'use client';

/**
 * Web-only stub: Capacitor is not available in this build.
 * Always returns false.
 */
export function useIsCapacitor(): boolean {
  return false;
}

/**
 * Synchronous check for Capacitor environment.
 * Always returns false in web-only build.
 */
export function isCapacitorEnvironment(): boolean {
  return false;
}
